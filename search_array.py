#here we will learn about the inserting and searching the number in array:
from array import *
#here we will talk about the inserting the number in the array from the users
x = array('i',[])
n = int(input("enter the length of the array:"))
for i in range(n):
    y = int(input("enter the next of the array:"))
    x.aapend(y)

print(x)   
